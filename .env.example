GATEWAY_PORT=3000

SSO_HOST=localhost
SSO_PORT=3001

BANKING_HOST=localhost
BANKING_PORT=3002

# Local DB (used when DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=tecopos
DB_PASSWORD=tecopos123
DB_NAME=tecopos_sso

# Neon (overrides DB_* when set)
# DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
# DATABASE_USE_IPV4=true

JWT_SECRET=your_jwt_secret_here
JWT_EXPIRATION=3600
